# OpenTenBase 架构术语表与新手导览

> 本文档面向 OpenTenBase 新手，用通俗易懂的语言解释核心架构概念，帮助你快速理解分布式数据库的工作原理。

---

## 📋 核心术语表（15 个）

### 1. Coordinator（协调节点，简称 CN）

**一句话解释**：数据库的"前台接待"，用户连接的入口。

**详细说明**：
- 你连接数据库时，实际上连的是 CN 节点
- 负责接收你的 SQL 请求，解析并制定执行计划
- 把任务分发给下面的数据节点（DN）去执行
- 最后把各个 DN 返回的结果汇总，返回给你
- **CN 不存真实数据**，只存系统元数据（比如表结构、数据分布规则）
- 可以有多个 CN，它们地位对等，连哪个都行

**打个比方**：CN 就像餐厅的服务员，你点菜（发 SQL）给服务员，服务员告诉厨房（DN）去做，做好了再端给你。

---

### 2. DataNode（数据节点，简称 DN）

**一句话解释**：数据库的"厨房"，真正存数据、算数据的地方。

**详细说明**：
- 实际存储用户数据的节点
- 数据会被分成多片，分别存在不同的 DN 上（分片存储）
- 负责执行 CN 分配下来的 SQL 任务
- 支持分片和复制两种模式
- 可以根据数据量水平扩展，加机器就能扩容

**打个比方**：DN 就像餐厅的厨房，菜（数据）都存在这里，厨师（CPU）在这里做菜（计算）。

---

### 3. GTM（Global Transaction Manager，全局事务管理器）

**一句话解释**：数据库的"时钟"和"裁判"，保证所有节点数据一致。

**详细说明**：
- 负责分配全局事务 ID（GXID）
- 管理分布式事务的提交和回滚
- 维护全局事务快照，保证 MVCC 一致性
- 管理集群的全局对象（比如序列 sequence）
- 整个集群的事务中枢，非常重要

**打个比方**：GTM 就像餐厅的收银台+时钟，所有订单（事务）都要从这里拿号，保证不会乱。

---

### 4. 分布式模式（Distributed Mode）

**一句话解释**：多机协同工作的模式，数据分散在不同节点上。

**详细说明**：
- 需要 GTM、Coordinator、DataNode 三类节点共同组成
- 数据分片存储在多个 DN 上
- 可以水平扩展，数据量越大越能体现优势
- 适合大数据量、高并发的场景
- OpenTenBase 的主要运行模式

**打个比方**：就像连锁餐厅，有很多分店（DN），顾客可以去任意一家，总部（GTM）统一管理。

---

### 5. 集中式模式（Centralized Mode）

**一句话解释**：单机运行模式，所有功能都在一个节点上。

**详细说明**：
- 不需要 GTM 和多个节点
- 所有数据和计算都在一个节点上
- 部署简单，适合学习、测试、小项目
- 功能和分布式模式基本一致
- 不能水平扩展

**打个比方**：就像单家小餐馆，老板（GTM）、服务员（CN）、厨师（DN）都是一个人干。

---

### 6. Node Group（节点组 / pgxc_group）

**一句话解释**：把多个 DN 节点编成一组，方便管理和调度。

**详细说明**：
- 数据节点的逻辑分组
- 可以创建默认组、分片组、复制组等不同类型
- 建表时可以指定放在哪个节点组
- 提高了集群管理的灵活性
- 所有 DN 默认都会加到默认组里

**打个比方**：就像厨房分了"川菜组"、"粤菜组"，不同菜系的菜交给不同组做。

---

### 7. opentenbase_ctl / pgxc_ctl

**一句话解释**：集群管理工具，一条命令就能管理整个集群。

**详细说明**：
- OpenTenBase 提供的命令行管理工具
- 可以启动、停止、重启整个集群
- 可以添加、删除节点
- 可以查看集群状态
- 不用一个个节点去操作，统一管理

**打个比方**：就像餐厅的总经理，一个人就能管所有分店的启停。

---

### 8. HTAP（Hybrid Transactional/Analytical Processing）

**一句话解释**：既能做交易又能做分析，一套系统干两套活。

**详细说明**：
- HT = 事务处理（OLTP）：比如下单、转账这种高频小操作
- AP = 分析处理（OLAP）：比如统计报表、大数据分析
- OpenTenBase 同时支持这两种 workload
- 不用再搞一套数据库专门做分析
- 企业级数据库的核心能力

**打个比方**：就像一家餐厅，既能做快餐（OLTP，快、量多），又能办宴席（OLAP，复杂、量大）。

---

### 9. Shared-Nothing（无共享架构）

**一句话解释**：每个节点都是独立的，各有各的 CPU、内存、磁盘。

**详细说明**：
- 分布式数据库的经典架构
- 节点之间不共享硬件资源
- 节点之间通过网络通信
- 扩展性好，加机器就能提升性能
- 没有单点瓶颈

**打个比方**：就像多家独立的餐厅，各有各的厨房和服务员，互不影响。

---

### 10. 分片（Sharding）

**一句话解释**：把一张大表拆成多份，分别存在不同节点上。

**详细说明**：
- 把数据按照某个规则（比如用户 ID）分成多片
- 每片存在不同的 DN 节点上
- 查询时只需要去相关的节点查，提高效率
- 水平扩展的核心手段
- 对用户透明，你感觉不到数据被拆了

**打个比方**：就像把一本厚字典按字母分成好几册，查的时候直接找对应的册子就行。

---

### 11. 元数据（Metadata）

**一句话解释**："数据的数据"，描述数据库结构的信息。

**详细说明**：
- 比如有哪些表、表有哪些字段、字段什么类型
- 数据分布在哪些节点上、怎么分布的
- 用户权限、索引信息等等
- CN 节点上存储的就是全局元数据
- 元数据本身不存业务数据

**打个比方**：就像图书馆的目录卡片，告诉你哪本书在哪个书架上。

---

### 12. 全局事务 ID（GXID）

**一句话解释**：每个分布式事务的"身份证号"，全局唯一。

**详细说明**：
- 由 GTM 统一分配
- 保证整个集群范围内事务 ID 不重复
- 用于分布式事务的协调和一致性判断
- MVCC 多版本并发控制的基础
- 分布式数据库的核心概念

**打个比方**：就像订单号，整个连锁餐厅的订单号都不能重复，不然就乱了。

---

### 13. MVCC（Multi-Version Concurrency Control，多版本并发控制）

**一句话解释**：读写不阻塞的秘诀，大家同时用也不打架。

**详细说明**：
- 每个数据都有多个版本
- 写的时候不阻塞读，读的时候不阻塞写
- 每个事务看到的是自己那个版本的数据
- 大大提高并发性能
- 现代数据库的标配技术

**打个比方**：就像文档的版本历史，你改你的，我看我的，互不影响。

---

### 14. 主备（Primary / Standby）

**一句话解释**：主节点干活，备节点待命，主节点挂了备节点顶上。

**详细说明**：
- 每个节点都可以有主节点和备节点
- 主节点负责提供服务
- 备节点实时同步主节点的数据
- 主节点故障时，备节点可以切换为主
- 保证高可用，不丢数据

**打个比方**：就像主角和替身，主角有事，替身马上顶上。

---

### 15. 复制（Replication）

**一句话解释**：同一份数据存多个副本，保证安全和读性能。

**详细说明**：
- 数据在多个 DN 上保存相同的副本
- 一个节点挂了，其他节点还有数据
- 读请求可以分散到多个副本，提高读性能
- 分为同步复制和异步复制
- 数据高可用的核心手段

**打个比方**：就像重要文件多备份几份，一份丢了还有其他的。

---

## 🏗️ 架构关系图（表格版）

### 核心组件关系表

| 组件 | 简称 | 干什么的？ | 存数据吗？ | 能有几个？ |
|---|---|---|---|---|
| **Global Transaction Manager** | GTM | 全局事务管理，分配事务 ID，保证一致性 | 存事务状态 | 一般 1-2 个（主备） |
| **Coordinator** | CN | 接收用户请求，SQL 解析，任务分发，结果汇总 | 只存元数据 | 可以很多个，对等 |
| **DataNode** | DN | 存储实际数据，执行计算任务 | 存真实业务数据 | 可以很多个，水平扩展 |

### 一次查询的完整流程

```
用户
  │
  │ 1. 发送 SQL 请求
  ▼
┌─────────────┐
│     CN      │  ← 协调节点
│ （服务员）   │
└──────┬──────┘
       │
       │ 2. 解析 SQL，制定执行计划
       │ 3. 分发任务到各个 DN
       ▼
┌──────┴──────┐
│  DN1  DN2  DN3  │  ← 数据节点
│ （厨房们）       │
└──────┬──────┘
       │
       │ 4. 各自执行本地计算
       │ 5. 返回结果给 CN
       ▼
┌─────────────┐
│     CN      │  ← 协调节点
│ （服务员）   │
└──────┬──────┘
       │
       │ 6. 汇总所有 DN 的结果
       ▼
     用户
```

### 事务的完整流程

```
用户开启事务
      │
      ▼
    GTM ←─────── 1. 申请全局事务 ID（GXID）
      │
      ▼
    CN  ←─────── 2. 带着 GXID 分发 SQL
      │
      ├───────── DN1 执行
      ├───────── DN2 执行
      └───────── DN3 执行
      │
      ▼
    CN  ←─────── 3. 收集各节点执行结果
      │
      ▼
    GTM ←─────── 4. 协调两阶段提交
      │
      ▼
  事务完成
```

---

## 🚀 新手快速理解口诀

> **一进二算三存底，全局事务靠 GTM**
>
> - **一进**：CN 是入口，所有请求先进 CN
> - **二算**：DN 负责计算，真正干活的
> - **三存底**：数据最终存在 DN 上
> - **全局事务靠 GTM**：GTM 管事务一致性

---

## 📚 延伸阅读建议

1. 先搞懂 CN、DN、GTM 三个核心组件
2. 再理解分布式事务怎么工作的
3. 然后学习数据分片和分布策略
4. 最后深入 HTAP 和性能优化

---

*本文档由犀牛鸟开源人才培养计划贡献者整理，面向新手友好*
