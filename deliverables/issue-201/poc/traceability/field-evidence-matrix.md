# Stage 4B 字段与证据追踪矩阵

分类沿用已审阅的四种标签。证据列使用最终交付文档、OpenTenBase 仓库源码路径或官方资料，读者无需访问内部调研目录即可理解。

| 文件 | 字段/概念 | 分类 | 证据 | 理由 | 所需运行时验证 |
|---|---|---|---|---|---|
| CRD | 顶层 `OpenTenBaseCluster` | Provisional PoC assumption | [`operator-design.md`](../../operator-design.md) 的“顶层资源”和“资源模型”；`README.md` 的 distributed 模式说明 | 一个边界使分布式部署及其有序收敛可审阅。 | 确认未来所有权和可能的子资源。 |
| CRD | GTM `primaryCount: 1` | Evidence-backed | [`research-report.md`](../../research-report.md) 的“OpenTenBase 部署模型”；`contrib/opentenbase_ctl/src/cluster/cluster.cpp` 的 distributed 初始化流程 | 已审查 bootstrap 首先初始化一个 GTM master；PoC 将数量固定为一。 | 确认生产 GTM 拓扑。 |
| CRD/样例 | 可选 GTM standby | Evidence-backed | [`research-report.md`](../../research-report.md) 的“主备与远程操作”；`contrib/opentenbase_ctl/src/types/types.h` 与 `contrib/opentenbase_ctl/src/cluster/cluster.cpp` 的 GTM master/slave 类型和初始化 | 工具使用独立初始化路径建模 GTM slave。 | 验证复制、持久化和生命周期。 |
| CRD/样例 | Coordinator `primaryCount` | Provisional PoC assumption | [`operator-design.md`](../../operator-design.md) 的“资源模型”；`README.md` 与 `contrib/opentenbase_ctl/src/types/types.h` 的 Coordinator 角色/主备说明 | 证据支持多个 CN master；显式 primary 数量是有边界的 PoC 表达。 | 验证数量边界、术语和客户端行为。 |
| CRD/样例 | Coordinator `standbyCountPerPrimary` | Provisional PoC assumption | [`operator-design.md`](../../operator-design.md) 的 Coordinator cardinality 说明；`contrib/opentenbase_ctl/src/cluster/cluster.cpp` 的同名 master/standby clone 流程 | 将已审查的同名 master/standby 配对显式表达为每个 primary 的 standby 数，不是经过证明的生产 cardinality API。总期望 CN 实例为 `primaryCount * (1 + standbyCountPerPrimary)`。 | 验证 fan-out、配对、clone 重试和恢复语义。 |
| CRD/样例 | DataNode shard 列表 | Evidence-backed | [`research-report.md`](../../research-report.md) 的“OpenTenBase 部署模型”和“扩缩容”；`contrib/opentenbase_ctl/src/cluster/cluster.cpp` 的 node/sharding group 创建流程 | 证据确认多个 DN master 和 node/sharding group。 | 验证 shard identity 约束和分布行为。 |
| CRD/样例 | 每个 DN shard 的 standby 数 | Evidence-backed | [`research-report.md`](../../research-report.md) 的“主备与远程操作”；`contrib/opentenbase_ctl/src/cluster/cluster.cpp` 的 DN standby clone 流程 | DN slave 与 DN master 不同，并从匹配 master clone。 | 验证 standby fan-out 和 shard 关联。 |
| 样例/计划 | 稳定角色和 shard 名称 | Provisional PoC assumption | [`operator-design.md`](../../operator-design.md) 的“稳定身份与网络”；`contrib/opentenbase_ctl/src/cluster/cluster.cpp` 的同名 master/standby 匹配 | 静态文件需要确定性逻辑身份。 | 验证数据库名称限制和替换行为。 |
| CRD/计划 | Headless 角色/shard discovery | Provisional PoC assumption | [`operator-design.md`](../../operator-design.md) 的“稳定身份与网络”和“首个可运行 PoC 的候选编排方式”；`src/include/catalog/pgxc_node.h` 的 host/port topology 字段 | 概念上需要稳定发现，但 Kubernetes 命名尚未测试。 | 验证每个 OpenTenBase 地址路径中的 DNS 解析。 |
| CRD/计划 | Coordinator 客户端 Service | Provisional PoC assumption | [`research-report.md`](../../research-report.md) 的“服务模型”；仓库 `README.md` 的客户端通过 Coordinator 接入说明 | 客户端通过 CN 接入；路由语义有意保持未定义。 | 验证 CN 选择、session、readiness 和客户端行为。 |
| CRD/计划 | GTM discovery Service | Provisional PoC assumption | [`operator-design.md`](../../operator-design.md) 的“稳定身份与网络”；`contrib/opentenbase_ctl/src/cluster/cluster.cpp` 的 CN/DN initdb GTM 参数 | CN/DN 初始化需要 GTM 坐标。 | 验证 hostname 和 endpoint 变化。 |
| CRD/样例 | 按角色存储意图 | Provisional PoC assumption | [`operator-design.md`](../../operator-design.md) 的“存储设计边界”；仓库 `README.md`、`src/include/catalog/pgxc_node.h` 与 GTM 初始化代码所示角色状态 | DN 持久性明确；CN/GTM 准确卷需求不完整，因此保守表达存储意图。 | 确定 PVC 布局、所有权、clone、恢复和替换语义。 |
| CRD | Bootstrap phase enum | Evidence-backed | [`research-report.md`](../../research-report.md) 的“当前分布式初始化顺序”；`contrib/opentenbase_ctl/src/cluster/cluster.cpp` 的 distributed 安装流程 | 已批准 phase 保留证据中的顺序并暴露部分进度。 | 证明 phase action、entry gate 和安全重试。 |
| CRD/status | `observedGeneration` | Provisional PoC assumption | [`operator-design.md`](../../operator-design.md) 的“Conditions 与状态”；[CloudNativePG 1.30 API 参考](https://cloudnative-pg.io/documentation/1.30/cloudnative-pg.v1/) | 将 Kubernetes 期望状态观测与数据库收敛分离。 | 定义未来 controller 更新规则。 |
| CRD/status | 期望/观测 topology generation | Provisional PoC assumption | [`operator-design.md`](../../operator-design.md) 的“拓扑收敛模型”；[`research-report.md`](../../research-report.md) 的“拓扑状态” | 当前工具没有 topology version；显式 generation 暴露分布式收敛。 | 定义 generation 计算和 drift 对比。 |
| CRD/status | 逐节点 applied generation | Provisional PoC assumption | [`operator-design.md`](../../operator-design.md) 的“拓扑收敛模型”；`contrib/opentenbase_ctl/src/cluster/cluster.cpp` 的逐 CN/DN master topology SQL | 每个必需 CN/DN master 独立接受 topology application。 | 验证 catalog 检查和部分重试逻辑。 |
| CRD/status | Pool reload 状态 | Evidence-backed | `src/backend/pgxc/pool/poolutils.c` 的 `pgxc_pool_reload()`；`contrib/opentenbase_ctl/src/cluster/cluster.cpp` 的 topology SQL 后 reload 调用 | 当前 bootstrap 在每个 master 的 topology SQL 后执行 pool reload。 | 验证成功检测与安全重试。 |
| CRD/status | Default/sharding group 状态 | Evidence-backed | [`research-report.md`](../../research-report.md) 的“当前分布式初始化顺序”；`contrib/opentenbase_ctl/src/cluster/cluster.cpp` 的 group 创建流程 | Group 创建是显式 post-bootstrap 操作。 | 验证 introspection、convergence 和 retry。 |
| CRD/status | Conditions 与 last error | Provisional PoC assumption | [`operator-design.md`](../../operator-design.md) 的“Conditions 与状态”和“Bootstrap 状态机” | 未来 reconciler 需要有边界的 readiness 和部分失败报告。 | 定义准确 condition transition 和保留策略。 |
| CRD/status | Unsupported operations | Explicitly out of scope | [`operator-design.md`](../../operator-design.md) 的“高可用设计边界”“扩缩容”和“备份恢复” | Status 必须暴露有意的 PoC 边界，而不是暗示支持。 | Stage 4B 不做运行时验证，后续范围再评估。 |
| 全部 | Hostname 兼容性 | Unconfirmed | [`operator-design.md`](../../operator-design.md) 的“稳定身份与网络”；`src/include/catalog/pgxc_node.h` 和 `contrib/opentenbase_ctl/src/cluster/cluster.cpp` 的 host/address 使用 | 已审查配置使用 host/address 字段，但未证明 DNS 名称行为。 | 测试 GTM、initdb、topology、replication 和 pool 路径。 |
| 计划 | `workloadPrimitive: Unselected` | Unconfirmed | [`operator-design.md`](../../operator-design.md) 的“工作负载管理策略”；[CloudNativePG 1.30 Pod 管理文档](https://cloudnative-pg.io/documentation/1.30/controller/) | 角色感知生命周期有价值，但尚未选择 OpenTenBase primitive。 | 评估直接 Pod、StatefulSet 和混合行为。 |
| CRD 边界 | 自动故障转移与 fencing | Explicitly out of scope | [`operator-design.md`](../../operator-design.md) 的“高可用设计边界”；[`research-report.md`](../../research-report.md) 的“高可用” | Promotion 和 promotion 后 topology 安全没有证据。 | 需要独立 HA 设计和破坏性故障测试。 |
| CRD 边界 | 分布式备份/恢复 | Explicitly out of scope | [`operator-design.md`](../../operator-design.md) 的“备份恢复”；[`research-report.md`](../../research-report.md) 的“备份恢复” | 单集群物理备份不能证明 OpenTenBase 分布式一致性。 | 定义一致性点、角色参与和恢复顺序。 |
| CRD 边界 | Resharding 与安全 shard 移除 | Explicitly out of scope | [`operator-design.md`](../../operator-design.md) 的“扩缩容”；`contrib/opentenbase_ctl/src/cluster/cluster.cpp` 的 node/sharding group 现有流程 | Replica 变化不等于 shard 数据重分布或 shrink。 | 需要数据移动和正确性设计。 |
| CRD 边界 | Major-version upgrade | Explicitly out of scope | [`operator-design.md`](../../operator-design.md) 的“分阶段实施建议”和“风险”；[`research-report.md`](../../research-report.md) 的未解决问题 | 混合版本 GTM/CN/DN 兼容性未确认。 | 建立协议/catalog 兼容性和升级顺序。 |
| CRD 边界 | 生产安全自动化 | Explicitly out of scope | [`operator-design.md`](../../operator-design.md) 的“安全迁移”；`contrib/opentenbase_ctl/src/node/node.cpp` 的现有 trust 配置路径 | 当前 SSH/IP/trust 假设未定义 Kubernetes 安全边界。 | 定义凭据、证书、RBAC 和网络认证。 |
| Status 样例 | 部分 topology 进度 | Provisional PoC assumption | [`operator-design.md`](../../operator-design.md) 的“拓扑收敛模型”和“PoC 交付与验证”；`contrib/opentenbase_ctl/src/cluster/cluster.cpp` 的并行 master 初始化与失败边界 | 部分状态展示逐 target 进度保留，不声称真实运行。 | 验证未来 controller observation 和 retry。 |
