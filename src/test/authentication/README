src/test/authentication/README

认证回归测试
===================================

该目录包含用于认证的测试套件。SSL 证书认证测试被单独保存在 src/test/ssl/ 中，因为它们更复杂，并且在多用户系统中运行不安全。

运行测试
=================

    make check

注意：这需要在 configure 中使用 --enable-tap-tests 参数。
