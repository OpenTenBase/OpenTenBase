src/tutorial/README

教程
========

该目录包含 SQL 教程脚本。要查看它们，首先执行以下命令编译所有脚本和用户定义函数和类型的 C 文件：
	% make
（需要使用 GNU make --- 在您的系统上可能命名不同，通常为 'gmake'）

然后，使用 -s（单步执行）标志运行 psql：
	% psql -s

从 psql 内部，您可以通过使用 psql 的 \i <filename> 命令尝试每个单独的脚本文件。
