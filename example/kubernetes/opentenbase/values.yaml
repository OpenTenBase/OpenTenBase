# Default values for OpenTenBase
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Related image configurations.
images:
  registry: docker.io
  repository: domainlau/opentenbase
  tag: v2.5.0
  pullPolicy: IfNotPresent